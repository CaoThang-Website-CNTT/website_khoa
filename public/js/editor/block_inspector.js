export class BlockInspector {
  renderTypographySettings(blockData, onChange) {
    const container = document.createElement('div');
    container.innerHTML = `<h4>Kiểu chữ</h4>`;
    return container;
  }
};